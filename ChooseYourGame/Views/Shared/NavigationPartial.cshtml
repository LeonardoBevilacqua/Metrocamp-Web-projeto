 <!-- NAV -->
    <nav>
        <div data-uk-sticky="sel-target: .uk-navbar-container; cls-active: uk-navbar-sticky">
            <nav class="uk-navbar-container nav-responsive" data-uk-navbar>
                <div class="uk-navbar-left">
                    <ul class="uk-navbar-nav">
                        <li class="nav-responsive-toggle">
                            <a class="uk-navbar-toggle" data-uk-navbar-toggle-icon href="#" data-uk-toggle="target: #offcanvas-push"></a>
                        </li>
                        <li class="@(ViewBag.title == "Inicio" ? "uk-active" : "uk-parent")">
                            <!-- cyg.com/home/main -->
                            <a asp-controller="Home" asp-action="Main">
                                <span data-uk-icon="home"></span> Inicio</a>
                        </li>
                        <li class="@(ViewBag.title == "Meu Perfil" ? "uk-active" : "uk-parent")">
                            <!-- cyg.com/Profiles -->
                            <a asp-controller="Profiles" asp-action="ViewProfile" asp-route-id="@User.Identity.Name">
                                <span data-uk-icon="user"></span> Meu perfil</a>
                        </li>
                    </ul>
                </div>
                <div class="uk-navbar-right">
                    <ul class="uk-navbar-nav">
                        <li class="uk-parent" id="search">
                            <div class="uk-margin">
                                <div class="uk-inline">
                                    <span class="uk-form-icon" data-uk-icon="icon:  search"></span>
                                    <form asp-controller="Search" asp-action="Index" method="GET">
                                        <input class="uk-input uk-form-blank" type="text" name="id" style="padding-left:35px">
                                    </form>
                                </div>
                            </div>
                        </li>
                        <li class="@(ViewBag.title == "Configurações" ? "uk-active" : "uk-parent")">
                            <!-- cyg.com/profiles/config -->
                            <a asp-controller="Profiles" asp-action="Config">
                                <span data-uk-icon="cog"></span> Configurações</a>
                        </li>
                        <li class="uk-parent">
                            <!-- cyg.com -->
                            <a asp-controller="Home" asp-action="LogOut">
                                <span data-uk-icon="sign-out"></span> Sair</a>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
        <div id="offcanvas-push" data-uk-offcanvas="mode: slide; overlay: true">
            <div class="uk-offcanvas-bar uk-width-1-1">
                <button class="uk-offcanvas-close" type="button" data-uk-close></button>
                <div class="uk-width-1-2@s uk-width-2-5@m">
                    <ul class="uk-nav uk-nav-default">
                        <li class="@(ViewBag.title == "Inicio" ? "uk-active" : "")">
                            <!-- cyg.com/home/main -->
                            <a asp-controller="Home" asp-action="Main">
                                <span data-uk-icon="home"></span> Inicio</a>
                        </li>
                        <li class="@(ViewBag.title == "Meu Perfil" ? "uk-active" : "")">
                            <!-- cyg.com/profiles -->
                            <a asp-controller="Profiles" asp-action="ViewProfile" asp-route-id="@User.Identity.Name">
                                <span data-uk-icon="user"></span> Meu perfil</a>
                        </li>
                        <li class="@(ViewBag.title == "Configurações" ? "uk-active" : "")">
                            <!-- cyg.com/profiles/config -->
                            <a asp-controller="Profiles" asp-action="Config">
                                <span data-uk-icon="cog"></span> Configurações</a>
                        </li>
                        <li>
                            <!-- cyg.com/home -->
                            <a asp-controller="Home" asp-action="LogOut">
                                <span data-uk-icon="sign-out"></span> Sair</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
    <!-- END NAV -->
